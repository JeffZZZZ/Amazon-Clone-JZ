# Amazon Clone, an eCommerce Web App build with MERN Stack

## To run this app locally

Install dependencies in root folder, backend folder and frontend folder:

`npm install`

Then run server and client concurrently:

`npm run dev`
